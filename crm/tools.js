function openPay(id){let o=db.opps.find(x=>x.id===id);modal(`<div class="row"><h3>Registrar pago · ${esc(o.company)}</h3><button class="btn" onclick="closeM()">✕</button></div><form class="fg" onsubmit="savePay(event,'${id}')"><label>Valor<input name="amount" type="number" required></label><label>Fecha<input name="date" type="date" value="${today()}"></label><label class="full">Nota<input name="note" placeholder="Anticipo, saldo final…"></label><div class="full"><button class="btn p">Registrar</button></div></form>`)}function savePay(e,id){e.preventDefault();let f=Object.fromEntries(new FormData(e.target));db.payments.push({id:uid(),opp:id,amount:+f.amount,date:f.date,note:f.note});save();closeM();render()}
function del(k,id){if(confirm('¿Eliminar registro?')){db[k]=db[k].filter(x=>x.id!==id);save();closeM();render()}}function wa(p,n){p=String(p).replace(/\D/g,'');if(p.length===10)p='57'+p;open(`https://wa.me/${p}?text=Hola%2C%20${n}%20te%20escribo%20desde%20CEGALAB.`,'_blank')}
function saveSet(){db.goal=+$('#goal').value||0;db.whatsapp=$('#wa').value;save();render()}function addService(){if(!$('#sn').value)return;db.services.push([$('#sn').value,+$('#sp').value||0]);save();render()}
function exportDB(){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(db,null,2)],{type:'application/json'}));a.download=`cegalab-crm-${today()}.json`;a.click()}function importDB(i){let f=i.files[0];if(!f)return;let r=new FileReader;r.onload=()=>{try{db=JSON.parse(r.result);save();render();alert('Respaldo importado')}catch{alert('Archivo inválido')}};r.readAsText(f)}function resetDB(){if(confirm('Esto borrará los datos guardados en este navegador.')){db=structuredClone(defaults);save();render()}}
$('#search').addEventListener('input',e=>{q=e.target.value;render()});nav();render();
