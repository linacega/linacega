function closeM(){$('#mb').classList.add('hidden')}function modal(h){$('#modal').innerHTML=h;$('#mb').classList.remove('hidden')}
function openOpp(id){let o=db.opps.find(x=>x.id===id)||{id:'',company:'',contact:'',phone:'',email:'',service:'Web Studio',amount:900000,stage:'Prospecto',prob:30,source:'Referido',next:'',date:today(),notes:''};modal(`<div class="row"><h3>${id?'Editar':'Nueva'} oportunidad</h3><button class="btn" onclick="closeM()">✕</button></div><form class="fg" onsubmit="saveOpp(event,'${id||''}')"><label>Empresa<input name="company" required value="${esc(o.company)}"></label><label>Contacto<input name="contact" value="${esc(o.contact)}"></label><label>WhatsApp<input name="phone" value="${esc(o.phone)}"></label><label>Correo<input name="email" type="email" value="${esc(o.email)}"></label><label>Servicio<select name="service" onchange="this.form.amount.value=this.selectedOptions[0].dataset.p">${db.services.map(s=>`<option data-p="${s[1]}" ${o.service===s[0]?'selected':''}>${esc(s[0])}</option>`).join('')}</select></label><label>Valor<input name="amount" type="number" required value="${o.amount}"></label><label>Etapa<select name="stage">${stages.map(s=>`<option ${o.stage===s?'selected':''}>${s}</option>`).join('')}</select></label><label>Probabilidad %<input name="prob" type="number" min="0" max="100" value="${o.prob}"></label><label>Origen<input name="source" value="${esc(o.source||'')}"></label><label>Fecha próxima acción<input name="date" type="date" value="${o.date||''}"></label><label class="full">Próxima acción<input name="next" value="${esc(o.next||'')}"></label><label class="full">Notas<textarea name="notes">${esc(o.notes||'')}</textarea></label><div class="full row"><button class="btn p">Guardar</button>${id?`<button class="btn r" type="button" onclick="del('opps','${id}')">Eliminar</button>`:''}</div></form>`)}
function saveOpp(e,id){e.preventDefault();let f=Object.fromEntries(new FormData(e.target));f.amount=+f.amount;f.prob=+f.prob;f.id=id||uid();let i=db.opps.findIndex(x=>x.id===f.id);i<0?db.opps.push(f):db.opps[i]=f;if(!db.contacts.some(c=>c.phone&&c.phone===f.phone)&&f.contact)db.contacts.push({id:uid(),name:f.contact,company:f.company,phone:f.phone,email:f.email});save();closeM();render()}
function openContact(id){let c=db.contacts.find(x=>x.id===id)||{id:'',name:'',company:'',phone:'',email:'',notes:''};modal(`<div class="row"><h3>Contacto</h3><button class="btn" onclick="closeM()">✕</button></div><form class="fg" onsubmit="saveContact(event,'${id||''}')"><label>Nombre<input name="name" required value="${esc(c.name)}"></label><label>Empresa<input name="company" value="${esc(c.company||'')}"></label><label>WhatsApp<input name="phone" value="${esc(c.phone||'')}"></label><label>Correo<input name="email" value="${esc(c.email||'')}"></label><label class="full">Notas<textarea name="notes">${esc(c.notes||'')}</textarea></label><div class="full row"><button class="btn p">Guardar</button>${id?`<button type="button" class="btn r" onclick="del('contacts','${id}')">Eliminar</button>`:''}</div></form>`)}
function saveContact(e,id){e.preventDefault();let f=Object.fromEntries(new FormData(e.target));f.id=id||uid();let i=db.contacts.findIndex(x=>x.id===f.id);i<0?db.contacts.push(f):db.contacts[i]=f;save();closeM();render()}
function openTask(id){let t=db.tasks.find(x=>x.id===id)||{id:'',title:'',company:'',date:today(),priority:'Media',done:false};modal(`<div class="row"><h3>Seguimiento</h3><button class="btn" onclick="closeM()">✕</button></div><form class="fg" onsubmit="saveTask(event,'${id||''}')"><label class="full">Acción<input name="title" required value="${esc(t.title)}"></label><label>Empresa<input name="company" value="${esc(t.company||'')}"></label><label>Fecha<input name="date" type="date" value="${t.date||''}"></label><label>Prioridad<select name="priority">${['Alta','Media','Baja'].map(x=>`<option ${t.priority===x?'selected':''}>${x}</option>`).join('')}</select></label><div class="full row"><button class="btn p">Guardar</button>${id?`<button type="button" class="btn r" onclick="del('tasks','${id}')">Eliminar</button>`:''}</div></form>`)}
function saveTask(e,id){e.preventDefault();let f=Object.fromEntries(new FormData(e.target));f.id=id||uid();f.done=db.tasks.find(x=>x.id===id)?.done||false;let i=db.tasks.findIndex(x=>x.id===f.id);i<0?db.tasks.push(f):db.tasks[i]=f;save();closeM();render()}function doneTask(id){let t=db.tasks.find(x=>x.id===id);t.done=!t.done;save();render()}
