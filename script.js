const menuBtn=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.textContent=open?'×':'☰'});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn?.setAttribute('aria-expanded','false');if(menuBtn)menuBtn.textContent='☰'}));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const prediag=document.getElementById('prediag-form');prediag?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('pd-name').value.trim();const city=document.getElementById('pd-city').value.trim();const need=document.getElementById('pd-need').value;const accounting=Number(document.getElementById('pd-accounting').value);const tax=Number(document.getElementById('pd-tax').value);const cash=Number(document.getElementById('pd-cash').value);const urgency=document.getElementById('pd-urgency').value;const score=accounting+tax+cash;let label='Bajo',cls='level-low',next='Una orientación inicial puede ser suficiente para definir el servicio adecuado.';if(score>=4){label='Alto';cls='level-high';next='Conviene revisar el caso con mayor detalle y validar documentación antes de cotizar el alcance.'}else if(score>=2){label='Medio';cls='level-mid';next='Hay señales que vale la pena revisar en una conversación de pre-diagnóstico.'}const result=document.getElementById('prediag-result');const text=`Hola WM Logistik. Soy ${name}${city?` de ${city}`:''}. Completé el pre-diagnóstico web. Mi principal necesidad es: ${need}. Urgencia: ${urgency}. Orientación inicial: nivel ${label}. Quiero continuar con el pre-diagnóstico gratuito.`;result.innerHTML=`<span class="level ${cls}">Orientación inicial: ${label}</span><strong>Siguiente paso recomendado</strong><p>${next}</p><a class="btn btn-orange" target="_blank" rel="noopener" href="https://wa.me/573245835162?text=${encodeURIComponent(text)}">Continuar por WhatsApp</a>`;result.classList.add('show')});document.getElementById('whatsapp-form')?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name').value.trim();const company=document.getElementById('company').value.trim();const challenge=document.getElementById('challenge').value;const msg=document.getElementById('message').value.trim();const text=`Hola WM Logistik, soy ${name}${company?` de ${company}`:''}. Quiero revisar: ${challenge}.${msg?` Contexto: ${msg}`:''}`;window.open(`https://wa.me/573245835162?text=${encodeURIComponent(text)}`,'_blank','noopener')});